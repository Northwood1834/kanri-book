# 共通バックアップ設計 v1

対象: `管理ブック` / `回線チェック`

## 1. 結論

- 常設の「データ統合アプリ」は作らない。
- 両アプリで同じ暗号化コンテナ形式を使う。
- v1では各アプリが自分のデータを保存・復元する。
- コンテナは複数データセットを格納できる形にしておき、将来は管理ブックから回線チェックも含めた1ファイルを書き出せるようにする。
- データを物理的に一本化する場合も、論理上は `kanri-book` と `kaisen-check` の名前空間を維持する。

別オリジンのブラウザ保存領域は統合アプリからも直接読めない。共通バックアップファイルを正式な受け渡し手段とする。

## 2. 利用者向けの操作

### 初回だけ

1. 設定から「バックアップを保存」を押す。
2. アプリが復元キーを生成する。
3. 復元カードを表示する。
4. 復元カードを保存したことを確認して、バックアップを書き出す。

復元キーは人が考えた文字列ではなく、暗号学的乱数から生成する。暗号強度を優先し、以前検討した6単語ではなく、BIP39日本語リストによる12単語（128-bitエントロピー＋チェックサム）を採用する。画面では3単語×4行にして読みやすくする。QRは補助表示に留め、12単語が正式な復元手段となる。

### 2回目以降

1. 「バックアップを保存」を押す。
2. OSの共有画面またはダウンロードが開く。

復元キーは暗号化済みのアプリデータ内に保持するため、毎回入力させない。端末のブラウザデータが消えた場合は、保存してある12単語を使う。

### 復元

1. 「バックアップから復元」を押す。
2. `.mmbak`ファイルを選ぶ。
3. 12単語を入力する。
4. 内容の概要を確認する。
5. 「この端末に復元」を押す。

復号に成功するまで、既存データには触れない。既存データがある場合は、置換される件数を表示して確認を取る。

## 3. バックアップ表示

設定画面には次だけを表示する。

```text
バックアップ
前回：7月24日
[バックアップを保存] [復元]
```

未作成の場合は「まだ保存されていません」。30日以上経過、または前回書き出し後に10回以上更新された場合だけ、設定アイコンに小さな印を付ける。モーダルを勝手に表示しない。

ブラウザはファイルが最終的に保存されたか確認できない場合がある。そのため内部名称は `lastExportAt` とし、UIでは「前回」とだけ表示する。「バックアップ済み」と断定しない。

## 4. ファイル形式

- 拡張子: `.mmbak`
- ファイル名: `mumupo-backup-YYYYMMDD.mmbak`
- MIME: `application/vnd.mumupo.backup+json`
- 文字コード: UTF-8
- 最大読込サイズ: v1では10 MiB
- 外側はJSON、暗号文はBase64url（パディングなし）

```json
{
  "format": "mumupo-backup",
  "version": 1,
  "createdAt": "2026-07-24T12:34:56.000Z",
  "backupId": "UUID",
  "crypto": {
    "cipher": "AES-256-GCM",
    "kdf": "PBKDF2-HMAC-SHA-256",
    "iterations": 1200000,
    "salt": "BASE64URL_16_BYTES",
    "iv": "BASE64URL_12_BYTES",
    "tagLength": 128
  },
  "ciphertext": "BASE64URL"
}
```

PBKDF2の反復回数はファイルに記録するが、読込時に無制限の値を受け入れない。v1の許容範囲は600,000〜2,000,000とする。書出し値は実端末で性能試験し、1,200,000を初期値とする。

## 5. 暗号化

1. Web CryptoのCSPRNGで128-bitの復元エントロピーを生成する。
2. BIP39日本語12単語へ変換する。
3. 入力時はUnicode NFKD正規化、全角・半角空白を区切りとして扱う。
4. 復元エントロピーをPBKDF2-HMAC-SHA-256へ入力する。
5. ランダム16-byte saltと規定反復回数からAES-256鍵を導出する。
6. ランダム12-byte IVでAES-256-GCM暗号化する。
7. 外側ヘッダーを固定順で直列化し、Additional Authenticated Dataへ入れる。

AADは次のUTF-8文字列を改行区切りで構成する。

```text
mumupo-backup
1
<createdAt>
<backupId>
AES-256-GCM
PBKDF2-HMAC-SHA-256
<iterations>
<salt>
<iv>
128
```

- 復元キーをバックアップファイルへ入れない。
- 復元キーや平文をlocalStorageへ入れない。
- 暗号化前のJSON、導出鍵、復号結果をログへ出さない。
- 書出しごとにsalt、IV、backupIdを新しく生成する。
- 復元エラーでは「復元キーまたはファイルを確認してください」とだけ表示し、判定理由を細分化しない。

## 6. 暗号化される中身

```json
{
  "format": "mumupo-backup-payload",
  "version": 1,
  "backupId": "外側と同じUUID",
  "createdAt": "外側と同じ日時",
  "datasets": [
    {
      "namespace": "kanri-book",
      "schemaVersion": 1,
      "exportedAt": "ISO-8601",
      "mode": "snapshot",
      "data": {}
    },
    {
      "namespace": "kaisen-check",
      "schemaVersion": 1,
      "exportedAt": "ISO-8601",
      "mode": "snapshot",
      "data": {}
    }
  ]
}
```

同じnamespaceは1ファイルに1件まで。未知のnamespaceや未対応schemaVersionは勝手に解釈せず、「このバージョンでは復元できません」とする。

## 7. データセット

### `kanri-book`

現在の復号済みドキュメントを格納する。

- `schema`
- `schemaVersion`
- `lines`
- `sets`
- `devices`
- `settings`
- `createdAt`
- `updatedAt`

端末内のAES鍵、画面ロック設定、IndexedDBの暗号化外枠は含めない。復元先では新しい非抽出端末鍵を生成し、復元データを再暗号化する。画面ロックも復元時に新しく決める。

### `kaisen-check`

既存キーをそのままコピーせず、明示した構造へ正規化する。

```json
{
  "schema": "kaisen-check",
  "schemaVersion": 1,
  "profiles": [],
  "checkHistory": [],
  "exportedFromLegacyKeys": true
}
```

- `profiles`は現在の`simProfiles`
- `checkHistory`は現在の`checkHistory`
- 旧`lastCheck`が残っている場合は書出し前に`checkHistory`形式へ正規化

履歴には将来、重複排除用の`eventId`を追加する。既存履歴には書出し時に決定的IDを付けず、そのまま保持する。曖昧な推測で別レコードを同一扱いしない。

## 8. IDと将来の一本化

- 管理ブックの`line.id`を変更しない。
- 回線チェックの`profile.id`を変更しない。
- 現在の`line.links.kaisenCheckProfileId`を正式な紐づけとして保持する。
- 電話番号を統合キーにしない。番号変更、重複記録、表記差があるため。

将来の物理的な共通保管でも、次の論理構造を維持する。

```text
secure vault
├ kanri-book       回線・端末・アカウント
├ kaisen-check     プロファイル・チェック履歴
└ integration      ID対応表と移行状態
```

回線チェックは暗証番号やアカウント情報を必要としない。チェック完了イベントだけを自分の名前空間へ書く。管理ブックは紐づけIDを使って印を表示する。

## 9. 復元と競合

v1の「復元」はマージではなく、そのアプリのnamespaceをスナップショットで置き換える。

1. ファイルサイズ、外側形式、バージョンを検査。
2. 復元キーを検査し、AES-GCM復号。
3. 外側と内側のbackupId・createdAt一致を検査。
4. JSON構造、件数上限、文字列長、ID重複を検査。
5. 復元内容を「回線22件、端末4件」のように表示。
6. 確認後、現在データの内部ロールバックを作る。
7. 1回のIndexedDB readwrite transactionで新しい暗号文へ置換。
8. 再読込・復号・件数照合が成功してからロールバックを削除。

回線チェックはlocalStorage更新が複数キーになるため、復元ジャーナルをIndexedDBへ先に書く。途中終了した場合、次回起動時に旧値へ戻すか新値を再適用できるようにする。

クロス端末の自動マージや「新しい方を自動採用」はv1で行わない。端末時計のずれや同時編集を誤って上書きするため。

## 10. 両アプリでの役割

### 管理ブック

- `kanri-book`を書き出す。
- 同一オリジンに回線チェックのデータがある場合、「回線チェックも含める」を表示できる。
- 将来の統合・移行操作は管理ブックの設定内に置く。

### 回線チェック

- `kaisen-check`だけを書き出す。
- 管理ブックの暗号化領域を読まない。
- 共通形式の管理ブックバックアップを選ばれても、未知namespaceとして破壊しない。

## 11. ブラウザごとの保存方法

1. `navigator.canShare({files})`が使える場合はWeb Share APIを優先。
2. 使えない場合はBlob URLと`download`属性を使う。
3. File System Access APIは補助機能にも採用しない。iPhone/Safariで一貫しないため。
4. 自動クラウド送信、外部API、テレメトリは追加しない。

共有画面やダウンロードは利用者操作を必要とする。暗号化ファイルの生成までは自動化できるが、端末外への保存完了を偽って表示しない。

## 12. 実装順序

1. 固定テストベクターを作る（復元エントロピー、salt、IV、AAD、期待暗号文）。
2. 共通コンテナの暗号化・復号・検証コードを実装。
3. 管理ブックへ保存・復元を実装。
4. 管理ブックで空データ、22件程度、上限付近、破損ファイルを試験。
5. 同じテストベクターで回線チェックへ実装。
6. 同一ファイルを両アプリで読み、namespace判定の互換性を確認。
7. 後から複数dataset書出しを有効化。

## 13. 必須試験

- 12単語の正規化、チェックサム、語順違い、1語違い
- 同じデータでも書出しごとにsalt・IV・暗号文が異なる
- ciphertext・tag・AAD・header改変をすべて拒否
- 10 MiB超、JSON深度超過、件数超過、重複IDを拒否
- 復号失敗時に既存データが1 byteも変わらない
- 復元途中でページを閉じても旧データか新データのどちらかに戻る
- 管理ブックの電話番号、PIN、メール、IMEIがファイル中に平文で存在しない
- 回線チェックのプロファイルIDと管理ブックのリンクIDが往復後も同じ
- iPhone Safari、Android Chrome、デスクトップChrome/Firefoxで保存・復元
- CSP下で外部通信が発生しない

## 14. 非目標

- 自動クラウド同期
- 世代管理、差分バックアップ
- 複数端末の自動マージ
- バックアップ専用の常設アプリ
- 復元キーのサーバー保管
- ブラウザ保存領域の場所を利用者に探させること
